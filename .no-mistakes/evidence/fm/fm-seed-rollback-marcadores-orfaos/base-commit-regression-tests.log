##### test_home_seed_warns_when_acquired_home_return_fails (base code) #####
do so (now or later) by using -c with the switch command. Example:
ok - home seed rollback warns when treehouse-acquired return fails
targeted run complete
EXIT=0
##### test_home_seed_clears_orphan_marker_from_returned_treehouse_home (base code) #####
do so (now or later) by using -c with the switch command. Example:
not ok - rollback left the returned slice's identity marker behind
EXIT=1
##### test_home_seed_restores_markers_after_reset_removes_staged_holds (base code) #####
do so (now or later) by using -c with the switch command. Example:
ok - home seed rollback restores staged markers from the backup after the return reset removes them
targeted run complete
EXIT=0
